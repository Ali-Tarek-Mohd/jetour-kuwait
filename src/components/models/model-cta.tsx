import Link from "next/link";
import { Container } from "@/components/ui/container";
import { JetourButton } from "@/components/ui/jetour-button";

export function ModelCta({ name, slug, brochure }: { name: string; slug: string; brochure?: string }) { return <section className="border-y border-white/10 bg-[#17130f] py-20 lg:py-24"><Container className="flex flex-col justify-between gap-8 lg:flex-row lg:items-end"><div><p className="eyebrow">Take the next step</p><h2 className="section-title max-w-3xl">Experience the {name}</h2><p className="mt-5 max-w-xl leading-7 text-white/58">Connect with Jetour Kuwait and make the next journey yours.</p></div><div className="flex flex-col gap-3 sm:flex-row lg:flex-wrap lg:justify-end"><JetourButton href={`/book-test-drive?model=${slug}`} size="large">Book a Test Drive</JetourButton><JetourButton href="/#contact" variant="secondary" size="large">Contact sales</JetourButton>{brochure ? <JetourButton href={brochure} variant="secondary" size="large">Download brochure</JetourButton> : null}<Link href="/models" className="inline-flex min-h-13 items-center justify-center border border-transparent px-5 text-xs font-semibold tracking-[.09em] uppercase hover:border-white/25">All models</Link></div></Container></section>; }
