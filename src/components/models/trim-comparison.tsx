import { Container } from "@/components/ui/container";
import type { VehiclePage } from "@/types/vehicle";

export function TrimComparison({ trims }: { trims: VehiclePage["trims"] }) { return <section id="trims" className="scroll-mt-36 bg-jetour-black py-20 lg:py-28"><Container><p className="eyebrow">Line-up</p><h2 className="section-title">Trims and availability</h2>{trims.length ? <><div className="mt-10 hidden overflow-hidden border border-white/10 md:block"><table className="w-full border-collapse text-left"><thead><tr className="bg-white/[.04]"><th className="p-5 text-xs uppercase">Trim</th><th className="p-5 text-xs uppercase">Key features</th><th className="p-5 text-xs uppercase">Availability</th></tr></thead><tbody>{trims.map((trim, i) => <tr key={trim.name ?? i} className="border-t border-white/10"><th scope="row" className="p-5">{trim.name ?? "To be confirmed"}</th><td className="p-5 text-sm text-white/55">{trim.features.join(" · ")}</td><td className="p-5 text-sm text-white/55">{trim.availability ?? "To be confirmed"}</td></tr>)}</tbody></table></div><div className="mt-8 grid gap-4 md:hidden">{trims.map((trim, i) => <article key={trim.name ?? i} className="border border-white/10 p-6"><h3 className="text-xl font-semibold">{trim.name ?? "To be confirmed"}</h3><p className="mt-4 text-sm leading-6 text-white/55">{trim.features.join(" · ")}</p><p className="mt-4 text-xs text-jetour-accent">{trim.availability ?? "Availability to be confirmed"}</p></article>)}</div></> : <div className="mt-10 border border-white/10 bg-jetour-surface p-8 sm:p-10"><h3 className="text-xl font-semibold uppercase">Kuwait trim details coming soon</h3><p className="mt-4 max-w-2xl text-sm leading-7 text-white/55">Confirmed trim names, equipment and pricing will be published here when official local information is available.</p></div>}</Container></section>; }
