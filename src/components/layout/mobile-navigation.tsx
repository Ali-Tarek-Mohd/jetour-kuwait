"use client";
import {useEffect,useRef,useState} from "react";import Link from "next/link";import {HugeiconsIcon} from "@hugeicons/react";import {Cancel01Icon,Menu01Icon} from "@hugeicons/core-free-icons";import {navigationItems} from "@/data/navigation";
export function MobileNavigation(){const[open,setOpen]=useState(false);const closeRef=useRef<HTMLButtonElement>(null);useEffect(()=>{if(!open)return;document.body.style.overflow="hidden";closeRef.current?.focus();const key=(e:KeyboardEvent)=>{if(e.key==="Escape")setOpen(false)};addEventListener("keydown",key);return()=>{document.body.style.overflow="";removeEventListener("keydown",key)}},[open]);return <div className="lg:hidden"><button type="button" aria-label="Open navigation menu" aria-expanded={open} aria-controls="mobile-menu" onClick={()=>setOpen(true)} className="grid size-12 place-items-center border border-white/25"><HugeiconsIcon icon={Menu01Icon} size={24}/></button>{open&&<div id="mobile-menu" role="dialog" aria-modal="true" aria-label="Navigation menu" className="fixed inset-0 z-[80] bg-jetour-black px-5 py-5 motion-safe:animate-[menu-in_.25s_ease-out]"><div className="flex items-center justify-between border-b border-white/12 pb-5"><span className="text-xl font-semibold tracking-[.16em]">JETOUR</span><button ref={closeRef} type="button" aria-label="Close navigation menu" onClick={()=>setOpen(false)} className="grid size-12 place-items-center border border-white/25"><HugeiconsIcon icon={Cancel01Icon} size={24}/></button></div><nav aria-label="Mobile navigation" className="mt-8 flex flex-col">{navigationItems.map(x=><Link key={x.label} href={x.href} onClick={()=>setOpen(false)} className="flex min-h-16 items-center border-b border-white/10 text-2xl font-medium">{x.label}</Link>)}</nav><div className="mt-8 grid gap-3"><Link href="/book-test-drive" onClick={()=>setOpen(false)} className="flex min-h-14 items-center justify-center bg-white font-semibold text-black uppercase">Book a Test Drive</Link><div className="flex min-h-12 items-center justify-center gap-5 text-sm"><button>English</button><span className="h-4 w-px bg-white/25"/><button lang="ar">العربية</button></div></div></div>}</div>}
