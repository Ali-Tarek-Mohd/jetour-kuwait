"use client";
import {useState} from "react";import Image from "next/image";import type {VehicleColor} from "@/types/vehicle";
export function VehicleColorPicker({colors}:{colors:VehicleColor[]}){const[selected,setSelected]=useState(0),color=colors[selected];return <div><div className="relative aspect-[16/8] w-full"><Image key={color.image} src={color.image!} alt={`Jetour T2 in ${color.name}`} fill sizes="(max-width: 1023px) 100vw, 60vw" className="object-contain motion-safe:animate-[hero-in_.3s_ease-out]"/></div><div role="radiogroup" aria-label="Vehicle color" className="mt-5 flex flex-wrap gap-4">{colors.map((x,i)=><button key={x.name} role="radio" aria-checked={i===selected} disabled={!x.available} onClick={()=>setSelected(i)} aria-label={`${x.name}${x.available?"":" — image unavailable"}`} className="group flex min-h-12 items-center gap-3 disabled:cursor-not-allowed disabled:opacity-35"><span style={{background:x.swatch}} className={`size-7 rounded-full border-4 ${i===selected?"ring-2 ring-jetour-accent ring-offset-4 ring-offset-black":"border-white/25"}`}/><span className="text-xs">{x.name}</span></button>)}</div><p className="mt-3 text-xs text-white/40">Additional finishes become selectable when approved local imagery is added.</p></div>}
