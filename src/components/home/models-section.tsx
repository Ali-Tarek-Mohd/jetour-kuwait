import {Container} from "@/components/ui/container";import {SectionHeading} from "@/components/ui/section-heading";import {vehicles} from "@/data/vehicles";import {ModelCard} from "./model-card";import {VehicleColorPicker} from "./vehicle-color-picker";
export function ModelsSection(){const featured=vehicles.find(v=>v.featured)!;return <section id="models" className="section-pad bg-jetour-black"><Container><SectionHeading eyebrow="Explore the range" title="Our models"/><div className="mt-12 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{vehicles.map((v,i)=><ModelCard key={v.slug} vehicle={v} index={i}/>)}</div><div id="t2" className="mt-20 grid items-center gap-10 border-t border-white/10 pt-16 lg:grid-cols-[.75fr_1.25fr]"><div><p className="eyebrow">Featured vehicle</p><h2 className="section-title">T2, in your element</h2><p className="mt-5 max-w-md leading-7 text-white/55">Explore the color architecture prepared for approved Jetour Kuwait paint imagery.</p></div><VehicleColorPicker colors={featured.colors}/></div></Container></section>}
